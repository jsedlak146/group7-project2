<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/public/css/style.css"/>
    <title>Quit Smoking in 90 Days!</title>
  </head>
  <body>

  <header>
    <div class="container-fluid text-bg-dark p-3">
      <div class="row">  
        <div class="col-12 col-sm-9">
          <h1 class="text-center display-4">Quit Smoking in 90 Days</h1>
        </div>
        <div class="col-12 col-sm-3 justify-content-md-end fs-4 pt-3 text-decoration-none text-white">
          <a class="text-decoration-none mx-2" href="/">Home</a>
          <a class="text-decoration-none mx-2" href="/stories">Stories</a>
              {{#if logged_in}}
          <a class="text-decoration-none mx-2" href="/profile">Profile</a>
              |
          <button class="no-button" id="logout">Logout</button>
              {{else}}
              <a class="text-decoration-none mx-2" href="/login">Login</a>
              {{/if}}
        </div>
      </div>
    </div>

</header>

        {{{body}}}
      </main>
    <footer class="text-bg-dark p-3">

        <h5 class="text-center">Made by Jon, Joe, Ben, and Tom</h5>
    </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>
</html>